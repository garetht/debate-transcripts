After making a change, run `npm exec -- tsc --noEmit` from within the `web` folder, and fix any type errors that result.

If typechecking of external imports fails due to the absence of type declarations, do not create them yourself. Ask for help instead.

After making a change, run `npm run test` from within the `web` folder, and fix any tests that fail.

